export class CommandHandler {
    // This method will handle the execution of commands
    public execute(command: any): void {
        // Logic to handle the command execution
        console.log(`Executing command: ${command}`);
    }
}